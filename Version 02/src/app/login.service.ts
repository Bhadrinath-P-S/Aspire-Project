import { Injectable } from '@angular/core';
import { of } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class LoginService {
  // Email:any="";
  // Password:any="";

  // Every data is in Reg Service 
}
